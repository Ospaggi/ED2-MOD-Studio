# 영웅전설 2 DOS판 역분석 메모 — v0.8.2

## 몬스터 레코드

몬스터 정의는 MON DLL 데이터 세그먼트 시작 부분의 64바이트 레코드로 저장됩니다.

```text
+0x00  레코드·배치 플래그 일부
+0x03  몬스터 레벨  uint8
+0x04  현재 HP       uint16
+0x06  최대 HP       uint16
+0x0A  공격력        uint16
+0x0C  원시 EXP      uint16
+0x0E  원시 Gold     uint16
+0x10  방어력        uint16
+0x12  마법          uint16
+0x16  속도          uint8
+0x17  행운          uint8
+0x30  CP949 이름
```

속도 `+0x16`과 행운 `+0x17`은 실제 전투 동작과 데이터 분포를 통해 확정 필드로 취급합니다.

## 활성 편성 플래그

레코드 첫 바이트의 최상위 비트가 설정된 슬롯은 기본 전투 편성에서 비활성입니다.

```text
(record[0] & 0x80) == 0  활성
(record[0] & 0x80) != 0  비활성
```

예를 들어 M_511은 4개 레코드가 있지만 앞의 사일런트 로드 2마리만 활성이고 뒤의 2개 슬롯은 비활성입니다.

## 보상 계산

일반 몬스터:

```text
floor(raw × 90 / 100)
```

M_505 이전에는 플레이어 평균 레벨과 몬스터 레벨 차이에 따른 감쇠가 추가됩니다.

```text
차이 +0~+2 : 100%
차이 +3    : 75%
차이 +4    : 50%
차이 +5    : 25%
차이 +6~   : 0%
```

M_505 이상은 레벨 감쇠가 강제로 100%입니다. BGM 8 전투는 90% 보정과 레벨 감쇠를 모두 건너뜁니다.

## 16비트 누적 오버플로

전투 중 EXP와 Gold 누적 변수는 16비트입니다.

```text
accumulator = (accumulator + monster_reward) & 0xFFFF
```

따라서 합계가 65,535를 넘으면 65,536 단위로 순환합니다.

v0.8.1은 저장 단계에서 각 DLL의 활성 편성을 계산하고, 오버플로가 예상되는 EXP·Gold 원시값을 비례 축소합니다. M_505 이전 그룹도 파티 레벨에 따라 100%가 적용될 수 있으므로 자동 보호는 가장 큰 보상인 100% 조건을 사용합니다.

## EXP 분배

전투 종료 후 EXP는 생존 파티원에게 올림 나눗셈으로 분배됩니다.

```text
share = ceil((battle_exp & 0xFFFF) / living_members)
```

Gold는 생존 인원으로 나누지 않습니다.

## 자동 조정 알고리즘

1. 활성 슬롯의 사용자가 요청한 원시값을 읽습니다.
2. BGM 8 여부를 반영해 누적 전 보상으로 변환합니다.
3. 합계가 65,535 이하라면 원시값을 유지합니다.
4. 초과하면 모든 활성 슬롯에 공통 정수 비율을 적용합니다.
5. 남는 누적 여유를 사용해 가능한 범위에서 원시값을 다시 증가시킵니다.
6. 비활성 슬롯은 계산에서 제외하고 요청값을 유지합니다.

개별 몬스터 편집에서는 다른 활성 몬스터의 현재 보상을 먼저 제외하고, 선택 슬롯에 들어갈 수 있는 최대 안전 원시값을 역산합니다.

## 마법 파라미터

`ED2MAIN.EXE`의 마법 레코드는 12바이트이며 마지막 두 바이트가 다음 역할을 합니다.

```text
+0x0A  bit7: MAGIC_SLC2 / bit0..6: MAGIC_BAIRITU
+0x0B  MAGIC_CNT
```

`MAGIC_BAIRITU`는 마법 효과 기준값에 적용되는 백분율 배율입니다. `100`이 기준 효과이며 실제 배율은 하위 7비트입니다.

`MAGIC_CNT`는 충전 카운터를 한 바이트에 패킹합니다. 하위 니블은 최초 카운트, 상위 니블은 이후 반복 카운트입니다. 두 니블이 같은 원본 값에서는 `0x11`, `0x22`, `0x55`, `0x66` 순으로 느려집니다. 어느 한 니블이 0이면 감소 과정에서 언더플로가 생길 수 있으므로 안정값으로 취급하지 않습니다.
